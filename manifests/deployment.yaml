apiVersion: apps/v1
kind: Deployment
metadata:
  name: inotify-repro
  namespace: default
  labels:
    app.kubernetes.io/name: inotify-repro
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: inotify-repro
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inotify-repro
    spec:
      containers:
        - name: inotify-repro
          image: ghcr.io/danielloader/inotify-mirrord-repro:main
          imagePullPolicy: Always
          env:
            - name: CONFIG_PATH
              value: /example/mount/path/config.json
          volumeMounts:
            - name: config
              mountPath: /example/mount/path
              readOnly: true
      volumes:
        - name: config
          secret:
            secretName: inotify-repro-config
